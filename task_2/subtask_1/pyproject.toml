[project]
name = "subtask-1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Adam Aly", email = "adamaly243@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
subtask-1 = "subtask_1:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
